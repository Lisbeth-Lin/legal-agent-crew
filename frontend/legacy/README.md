# Legacy Streamlit Prototype

The existing Streamlit prototype is preserved at the repository root as `app.py`.

W1 does not move or rewrite it. Future frontend work should introduce an HTTP-only Streamlit
client under `frontend/` and can migrate legacy behavior deliberately.
